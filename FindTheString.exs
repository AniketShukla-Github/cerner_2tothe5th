#cerner_2tothe5th_2022
#cerner_2^5_2022

regex = ~r/cerner|cares/
IO.puts("cerner is working in healthcare sector" =~ regex)
IO.puts("it cares for the society " =~ regex)
IO.puts("This code is being submitted as part of 2^5 coding challenge" =~ regex)
